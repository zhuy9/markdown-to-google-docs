# Code blocks

Inline `value = 1` remains selectable.

```python
def greet(name):
    message = f"Hello, {name}"

    return message
```

````text
Literal fenced Markdown:
```python
print("not another block")
```
![not an image](literal.png)
````
